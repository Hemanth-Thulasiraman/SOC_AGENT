server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    # HashRouter is used (routes are /#/...), so no SPA fallback rewrite is
    # needed -- every route resolves to the same static index.html anyway.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
